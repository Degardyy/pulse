<?php

// PULSE registers all routes from its modules (modules/<Name>/routes/web.php),
// loaded by App\Modules\ModuleServiceProvider. Keep application-wide routes here only
// if they belong to no module — which should be rare.
